import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Hyperlink } from ".";

<Meta title="Hyperlink" />

export const Template = (args) => <Hyperlink {...args} />;

# Hyperlink

The Hyperlink component is a plain ol' anchor hyperlink, with some nice styling thrown in. Its default appearance is very minimal, with some box-shadow color changes when interacted with.

## Basic Usage

<Canvas>
  <starstuff-hyperlink href="#">Test</starstuff-hyperlink>
</Canvas>

## Props

<!-- {/_ <ArgsTable of={Hyperlink} /> _/} -->
